const config = {
    DIFF_U: 0.9,
    DIFF_V: 0.1,

    MIN_FEED_RANGE: 0.03,
    MAX_FEED_RANGE: 0.12,

    MIN_KILL_RANGE: 0.045,
    MAX_KILL_RANGE: 0.055,

    CELL_SIZE: 5,

    DEFAULT_BACKGROUND_COLOR: "#000000",
    DEFAULT_DOT_COLOR: "#FFFFFF",
}

export default config;